extends layout

block content
    .row
        .col-12.col-md-8
            .container
                .row
                .col
                    h1.text-center Chess Games
                    form(action='chess', method='POST')
                        select(name='')
                            each player in players
                                option(value=player) #{player}
                        input(type='submit', value='Submit')
                    

                .row
                    .col
                        .table-responsive
                        table.table.table-sm.table-hover
                            thead
                                tr
                                    th Url
                                    th Rules
                                    th Rated
                                    th Time Class
                                    th White Result
                            tbody
                                each chosenPlayer in chosenPlayers
                                    tr
                                        td #{game.games.url}
                                        td #{game.games.rules}
                                        td #{game.games.rated}
                                        td #{game.games.timeclass}
                                        td #{game.games.Whiteresult}

